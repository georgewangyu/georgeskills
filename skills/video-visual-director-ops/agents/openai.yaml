interface:
  display_name: "Video Visual Director"
  short_description: "Direct the post-A-cut visual edit"
  default_prompt: "Use $video-visual-director-ops to direct and execute the visual pass for this locked A-cut."
policy:
  allow_implicit_invocation: true
