# IMPROVEMENTS

## Backlog

- Add a root-level skill quality checklist that can be reused during PR review.
- Add a lightweight script that validates each skill has `Trigger`, `Workflow`,
  and `Output Contract` sections.
- Add a docs index page mapping each skill to its private-repo dependencies and
  expected outputs.
- Add periodic stale-skill detection for skills that have not been touched in
  long windows but still appear in active workflows.
- Add a compatibility matrix for key external CLIs used by skills (version
  ranges and known breakpoints).
